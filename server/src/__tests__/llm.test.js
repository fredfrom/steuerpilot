import { generateAnswer } from "../services/llm.js";

describe("llm service", () => {
  describe("generateAnswer", () => {
    it.todo("should return a string answer");
    it.todo("should include context chunks in the prompt");
    it.todo("should call Mistral API with correct model");
    it.todo("should handle API errors gracefully");
    it.todo("should generate answer in German");
  });
});
